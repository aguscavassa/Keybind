<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Keybind.Front.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Keybind.Front"
    xmlns:Services="using:Keybind.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">

    <StackPanel CornerRadius="8" Margin="48,12,48,12" >
        <StackPanel.Style>
            <Style TargetType="StackPanel">
                <Setter Property="Background" Value="#0FFFFFFF" />
            </Style>
        </StackPanel.Style>
        <controls:DataGrid x:Name="MainDataGrid"
                           Margin="12" AutoGenerateColumns="True"
                           ItemsSource="{x:Bind Services:CollectionManagement.ServiceCollection}"
                           IsReadOnly="True" SelectionMode="Single" CurrentCellChanged="MainDataGrid_CurrentCellChanged">
        </controls:DataGrid>
    </StackPanel>
</Page>
